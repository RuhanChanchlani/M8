# firebase_config.py — Initialize Firebase Admin SDK
#
# TODO:
#   - Import firebase_admin and credentials
#   - Build a credentials dict from environment variables (not a JSON file)
#   - Call firebase_admin.initialize_app(cred, {"databaseURL": "..."})
#   - Export db = firebase_admin.db  for use in other services
