// App.jsx — Root component, defines all routes
//
// TODO: Set up React Router routes:
//
//   /login           → LoginPage (public)
//   /sos             → GuestSOSPage (public — no auth required)
//   /dashboard       → DashboardPage (protected — manager only)
//   /responder       → ResponderPage (protected — staff)
//   /audit           → AuditPage (protected — manager only)
//   *                → NotFoundPage
//
// Wrap protected routes with <ProtectedRoute /> component
// Import and apply global styles from ./styles/index.css
